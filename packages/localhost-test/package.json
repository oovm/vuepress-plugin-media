{
    "name": "localhost-test",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "dev": "vuepress dev .",
        "build": "vuepress build . -dest ./dist",
        "start": "yarn dev && start http://localhost:8080/zh/"
    },
    "dependencies": {
        "vuepress-plugin-aplayer": "0.1.0"
    },
    "devDependencies": {
        "vuepress": "^1.0.0-alpha.30"
    }
}
